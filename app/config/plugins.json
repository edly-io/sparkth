{
  "plugins": {
    "_comment": "Example plugin configuration - uncomment and modify to add your plugins",
    "_format": "module attribute should be in format 'module.path:ClassName'",
    "canvas-plugin": {
      "module": "sparkth-plugins.canvas.plugin:CanvasPlugin",
      "enabled": true,
      "config": {
      }
    }
  }
}
