{
  "plugins": {
    "_comment": "Example plugin configuration - uncomment and modify to add your plugins",
    "canvas-plugin": {
      "module": "sparkth-plugins.canvas.plugin",
      "class": "CanvasPlugin",
      "enabled": true,
      "config": {
        "default_page_size": 10,
        "timeout": 30
      }
    }
  }
}
