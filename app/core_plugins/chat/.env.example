# Chat Plugin Configuration

# OPTIONAL (with defaults)
CHAT_RATE_LIMIT_REQUESTS_PER_MINUTE=60
CHAT_RATE_LIMIT_CHAT_PER_MINUTE=10
CHAT_RATE_LIMIT_CONCURRENT_STREAMS=5
CHAT_MAX_TOKENS_PER_REQUEST=4096
CHAT_DEFAULT_TEMPERATURE=0.7
CHAT_MAX_CONVERSATION_HISTORY=50

# Generate encryption key:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"